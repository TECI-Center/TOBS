"""TOBS - Tap Onto Both Sources.

A cross-platform (Windows + macOS) desktop app to view and record cameras
side-by-side: a USB GoPro Hero 8 (Webcam mode) and an ORDRO EP8 streaming over
its own Wi-Fi (RTSP/HTTP). A live timestamp is burned into both the preview and
the recordings.
"""

__version__ = "0.1.0"
